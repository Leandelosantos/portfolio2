// ExhibicionesPage.jsx — Exhibiciones (Figma nav link)
// Implementado en FASE 5

export default function ExhibicionesPage() {
  return (
    <main id="main-content" style={{ minHeight: '100vh', padding: 'clamp(100px, 12vh, 140px) clamp(24px, 6vw, 80px) 4rem', color: 'var(--color-text-secondary)', fontFamily: 'var(--font-mono)', fontSize: 'var(--type-mono)', letterSpacing: 'var(--ls-mono)' }}>
      EXHIBICIONES — FASE 5
    </main>
  );
}
